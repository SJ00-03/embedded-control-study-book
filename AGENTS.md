# AGENTS.md

이 저장소는 한국어 임베디드 제어 소프트웨어 학습서를 장기간 생성하기 위한 프로젝트입니다. Codex Web이 한 번에 한 lesson 또는 lab을 Pull Request로 추가하는 흐름을 전제로 합니다.

## 기본 원칙

- 주요 교육 본문은 한국어로 작성합니다.
- 독자는 전기/전자공학 전공 졸업자 또는 이에 준하는 배경을 가진 임베디드 제어 SW 취업 준비생입니다.
- 설명은 실무 구현 중심으로 작성하되, 기초 개념을 건너뛰지 않습니다.
- 저작권이 있는 교재, 강의안, 문서의 문장을 복제하지 않습니다. 설명, 예제, 실습, 문제는 새로 작성합니다.
- 클라우드 및 GitHub 환경에서 리뷰·빌드·테스트 가능한 구성을 우선합니다.
- Codex Windows App, WSL2, STM32CubeIDE, MATLAB 데스크톱, ROS 2 데스크톱 설치를 전제로 하지 않습니다.

## 안전 경계

방산 분야 언급은 다음 범위로 제한합니다.

- 신뢰성, 실시간 제약, 안전 중요 소프트웨어 개발 규율
- 검증, 테스트, 문서화, 추적성, 코드 리뷰
- 방어적 코딩, 코딩 표준, 장애 분석

다음 내용은 포함하지 않습니다.

- 무기 표적화, 회피, 공격적 군사 알고리즘
- 위해를 가능하게 하는 작전 절차나 구체적 운용 지침
- 실제 무기체계 성능 개선을 직접적으로 돕는 민감 구현 세부사항

## 저장소 영역별 역할

- `book/`은 lesson 본문을 보관하는 공간입니다. 개념 설명, 예제, 실습 안내, 확인 문제, 심화 과제를 담되 full solution이나 모범 답안은 넣지 않습니다.
- `study_notes/`는 학습자가 직접 작성하는 개인 답안과 오답노트 공간입니다. 사용자가 명시적으로 요청하지 않는 한 기존 파일을 덮어쓰거나 대규모로 재작성하지 않습니다.
- `feedback/`은 Codex가 학습자의 `study_notes/` 답안을 검토한 결과를 저장하는 공간입니다. 문항별 강점, 보완점, 다시 풀 질문, 오답 정리 제안을 담습니다.
- `solutions/`는 학습자가 자기 답안을 작성한 뒤 비교할 수 있는 모범 답안과 해설을 저장하는 공간입니다.
- `quizzes/`는 lesson에서 분리해 관리할 확인 문제 모음이 필요할 때 사용합니다.
- `labs/`는 빌드 또는 실행 가능한 실습 코드와 안내를 보관합니다.
- `prompts/`는 Codex Web에서 반복 사용할 작업 지시문을 보관합니다.

## 학습 답안 검토 워크플로 규칙

- 학습자는 먼저 `book/`의 lesson을 읽고 `study_notes/`에 자기 답을 작성합니다.
- Codex는 `study_notes/` 파일이 존재하고 학습자의 답안 내용이 확인된 뒤에만 `feedback/`과 `solutions/`를 생성합니다.
- 빈 템플릿만 있는 상태에서는 full solution, 모범 답안, 문항별 정답을 만들지 않습니다.
- 사용자가 명시적으로 요청하지 않는 한 `study_notes/`의 기존 답안, 오답노트, 실습 기록을 덮어쓰지 않습니다.
- `book/`에는 정답 또는 해설을 추가하지 않습니다. lesson은 문제를 풀기 전 학습 본문으로 유지합니다.
- `feedback/`에는 피드백과 복습 방향을, `solutions/`에는 별도 모범 답안을 분리해 작성합니다.

## Lesson 작성 규칙

모든 lesson은 아래 12개 절을 같은 순서로 포함해야 합니다.

1. 학습 목표
2. 왜 이 개념이 임베디드 제어 SW에서 중요한가
3. 기초 개념
4. 공식 또는 이론의 유도/직관
5. 간단한 예시
6. 실무형 예시
7. 직접 구현 실습
8. 디버깅 포인트
9. 방산/로봇 SW 관점에서의 주의점
10. 확인 문제
11. 심화 과제
12. 포트폴리오 연결 아이디어

## 실습 및 코드 규칙

- C 예제는 Linux 클라우드 환경에서 GCC와 `make`로 실행 가능하게 작성합니다.
- STM32 내용은 보드 독립적인 개념 설명을 먼저 제시하고, 보드별 구현은 별도 참고로 분리합니다.
- ROS 2 내용은 특정 로봇 플랫폼을 가정하지 않습니다.
- MATLAB/Control 예시는 MATLAB Online 호환성을 우선 고려하고, 가능한 경우 Octave 친화적인 설명도 덧붙입니다.
- Python 자동화 예시는 표준 라이브러리 또는 가벼운 의존성을 우선합니다.
- 저장소는 가볍게 유지하며 대용량 바이너리, 생성물, IDE 프로젝트 파일을 커밋하지 않습니다.

## Pull Request 작성 규칙

PR은 다음을 명확히 설명해야 합니다.

- 추가/수정한 lesson 또는 lab의 범위
- 실행한 품질 검사와 빌드/테스트 명령
- 다음에 생성하기 좋은 lesson 또는 lab
